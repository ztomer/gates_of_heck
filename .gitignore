__pycache__/
.DS_Store
.opencode/
*.pyc

# Rust build output (builds normally use the shared CARGO_TARGET_DIR;
# a repo-local target/ must never be committed)
/target/

# The native binary install.sh builds here; never committed.
/bin/goh
